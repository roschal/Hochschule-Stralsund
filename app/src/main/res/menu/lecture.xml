<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item
        android:id="@+id/action_add_termin"
        android:icon="@drawable/ic_add"
        android:title=""
        android:tint="@android:color/white"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_edit_lecture"
        android:icon="@drawable/ic_edit_black_24dp"
        android:title="Veranstaltung bearbeiten"
        android:tint="@android:color/white"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_delete_lecture"
        android:icon="@drawable/ic_delete_black_24dp"
        android:tint="@android:color/white"
        android:title="Veranstaltung löschen"
        app:showAsAction="never" />
</menu>